# ============================================================================
# IronPulse environment
#
# Copy this file to `.env.local` and fill in what you need.
# Everything here is OPTIONAL — with no keys at all the app runs in local mode
# (data in browser storage) and the AI coach falls back to its offline analyst.
# ============================================================================

# ---------------------------------------------------------------- Firebase --
# Create a project at https://console.firebase.google.com, add a Web App, then
# enable Authentication (Email/Password + Google) and Cloud Firestore.
# Paste the config values from Project settings → Your apps → SDK setup.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# --------------------------------------------------------------- AI coach --
# Server-side only — never prefixed with NEXT_PUBLIC.
# Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=
